http://codeforces.com/problemset/problem/1009/B
Minimum Ternary String
------------------------------------------------------------------------------------------------------------------------------------------
题目意思：
给你一个由0，1，2组成的字符串
可以将相邻的0和1或1和2交换，但交换的次数没有限制。
最后希望可以得到一个最小的字符串。
当然也可以选择不变。
------------------------------------------------------------------------------------------------------------------------------------------
解题思路：
由题意可知1是关键————因为1既可以与0换，也可以与2换。
因此，要按照原字符串的顺序把0和2与1分开。
因为要得到最小的结果，则要使0尽可能地往前面放，2尽可能地往后面放。
因此，如果在t(由0和2按照原字符串的顺序组成的)中遇到2，则要使1全到它的前面。
如果有一个2在1的前面就会影响到字符串的大小。
------------------------------------------------------------------------------------------------------------------------------------------
伪代码：
for (int i=0;i<s.size();i++){
	 if (s[i]=='1') //把1归在一块
	 if (s[i]!='1') //把0和2归在一块
}
for (int i=0;i<t.size();i++){
	if (//如果遇到了2) //让所有的1数出来，使它们到2前面。
	 //把0和2按序输出
}
